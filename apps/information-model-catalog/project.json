{
  "name": "information-model-catalog",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/information-model-catalog",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/information-model-catalog"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/information-model-catalog"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "information-model-catalog:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "information-model-catalog:build:development",
          "dev": true
        },
        "e2e": {
          "buildTarget": "information-model-catalog:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "information-model-catalog:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "information-model-catalog:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/information-model-catalog/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "apps/information-model-catalog/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  },
  "tags": []
}
