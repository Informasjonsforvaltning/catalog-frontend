export type Params = {
  params: {
    catalogId: string;
  };
};

export type DatasetParams = {
  params: {
    catalogId: string;
    datasetId: string;
  };
};

export type DataServiceParams = {
  params: {
    catalogId: string;
    dataServiceId: string;
  };
};
