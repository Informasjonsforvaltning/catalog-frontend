export enum EntityEnum {
  CONCEPT = 'concept',
  EVENT = 'event',
}

export enum PublicationStatus {
  DRAFT = 'DRAFT',
  PUBLISH = 'PUBLISH',
  APPROVE = 'APPROVE',
}

export enum SpecializedType {
  SERIES = 'SERIES',
}
