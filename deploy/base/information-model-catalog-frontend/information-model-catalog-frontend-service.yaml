---
apiVersion: v1
kind: Service
metadata:
  name: information-model-catalog-frontend
spec:
  selector:
    app: information-model-catalog-frontend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
